version: "3"

services:
  vespa-php:
    container_name: vespa-php
    build:
      context: ./
      dockerfile: docker/Dockerfile
    working_dir: /vespa-php
    entrypoint: /usr/local/bin/run-server
    volumes:
      - ./:/vespa-php:cached
    expose:
      - 9000
